{
  "name": "RhythmVAE",
  "version": "1.0.0",
  "main": "rhythmvae.js",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "scripts": {
    "build": "npx esbuild rhythmvae.js --bundle --platform=node --format=cjs --target=node18 --external:max-api --external:aws-sdk --external:mock-aws-s3 --external:nock --loader:.html=copy --outfile=patchers/bundled_rhythmvae.js"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.22.0",
    "@tonejs/midi": "^2.0.28",
    "cross-fetch": "^4.1.0",
    "glob": "^13.0.0",
    "tiny-glob": "^0.2.9"
  },
  "description": "Max for Live(M4L) Rhythm generator using Variational Autoencoder(VAE)",
  "devDependencies": {
    "esbuild": "^0.27.1"
  }
}
